{% extends 'base.html.twig' %}

{% block title %}Редактирование дисциплины{% endblock %}

{% block body %}
<h2>Редактирование дисциплины</h2>

<div class="mb-3">
    <a href="{{ path('teacher_dashboard') }}" class="btn btn-secondary">Вернуться назад</a>
</div>

{{ form_start(form) }}
    <div class="mb-3">
        {{ form_label(form.name, 'Discipline Name') }}
        {{ form_widget(form.name, {'attr': {'class': 'form-control'}}) }}
    </div>
    <button type="submit" class="btn btn-primary">Сохранить</button>
{{ form_end(form) }}
{% endblock %}
